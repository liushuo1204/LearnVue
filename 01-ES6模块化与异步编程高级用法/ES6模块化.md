# 1.回顾：node.js 中如何实现模块化
node.js遵循了`CommonJS`的模块化规范。其中：  
- 导入其它模块使用`require()`方法
- 模块对外共享成员使用`module.exports`对象

模块化的好处：
大家都遵守同样的模块化规范写代码，`降低了沟通的成本，极大方便了各个模块之间的相互调用`，利人利己。
# 2.前端模块化规范的分类
在`ES6模块化规范`诞生之前，JavaScript社区已经尝试提出了`AMD`